# Behavioral Training Rig

A behavioral training platform controller: an Arduino-based firmware (`firmware/behavioral_controller`)
drives wheel, brake, lick, spout, and speaker hardware for a behavior session, while a Python
host application (`behavioral_master.py`) logs session data and syncs it to Google Sheets for
live dashboarding.

Documentation is in progress.
